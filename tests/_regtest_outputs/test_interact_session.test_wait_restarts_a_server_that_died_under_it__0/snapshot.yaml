# A wait revives its server, reports recovery on stderr, and delivers input.
# The URL's dynamic port and access token are represented by <served-url>.

exit: 0

stderr: |
  <page>: server had died; restarted at <served-url>
