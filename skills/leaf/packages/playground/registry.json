{
  "lf-playground": {
    "description": "A reusable interactive playground that turns declarative controls into one local value set, reflects it into one operable preview and authored instruction, and sends the final configuration as one `choose` action. Put it inside lf-ask when the configuration is a question for the reader. Direct children are controls, presets, one preview, and one output. An A/B preview keeps both candidates together so one control edit or preview gesture can drive both from the same input. Control edits, preview gestures, and presets stay in this browser tab; Use these settings records the complete typed value map. CSS previews read `--playground-NAME` and `data-playground-NAME` on this element. Page modules can read the public `values` property and listen for `lf-playground-change` when behavior or computation requires JavaScript.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "submit-label": {
        "type": "string",
        "minLength": 1
      },
      "restated": {
        "type": "boolean"
      }
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-content": "items",
    "x-guidance": {
      "author": "Use controls for candidates and parameters the reader should vary together. Keep A/B candidates in the same preview and apply each control edit or custom gesture to both from one shared state; show live measurements when they affect the choice. Wrap the real artifact and load its vendored or page-media assets from the page's origin. Bind simple changes with `--playground-NAME` or `data-playground-NAME`, and use an inline page module for behavior or computation. Write a self-contained task in `lf-playground-output`; `choose` sends every typed control value whether or not the prose repeats it."
    },
    "x-upgrade": true,
    "x-awaits": {
      "region": true,
      "answers": ["choose"]
    },
    "x-state": {
      "choose": {
        "detail": {
          "type": "object",
          "properties": {
            "values": {
              "type": "object",
              "minProperties": 1,
              "propertyNames": {
                "pattern": "^[a-z][a-z0-9-]*$"
              },
              "additionalProperties": {
                "type": ["string", "number", "boolean"]
              }
            },
            "instruction": {
              "type": "string",
              "minLength": 1
            }
          },
          "required": ["values", "instruction"],
          "additionalProperties": false
        },
        "facet": "configuration",
        "unit": "widget"
      }
    },
    "x-example": "<lf-ask id=\"notification-ask\"><h2>Which notification should we build?</h2><lf-playground id=\"notification-playground\" submit-label=\"Create notification\"><lf-playground-control name=\"format\" label=\"Format\" kind=\"choice\" value=\"banner\"><lf-playground-choice value=\"banner\" label=\"Banner\"></lf-playground-choice><lf-playground-choice value=\"status strip\" label=\"Status strip\"></lf-playground-choice></lf-playground-control><lf-playground-control name=\"radius\" label=\"Corner radius\" kind=\"range\" value=\"10\" min=\"0\" max=\"24\" step=\"1\" unit=\"px\"></lf-playground-control><lf-playground-preview><section aria-label=\"Notification candidates\"><article data-candidate=\"banner\"><h3>Banner</h3><p>The real banner responds to the shared controls.</p></article><article data-candidate=\"status strip\"><h3>Status strip</h3><p>The real status strip responds to the same controls.</p></article></section></lf-playground-preview><lf-playground-output>Build the <lf-playground-value for=\"format\"></lf-playground-value> notification with <lf-playground-value for=\"radius\"></lf-playground-value> corners. Replace the notification component and show me its browser test.</lf-playground-output></lf-playground></lf-ask>"
  },
  "lf-playground-control": {
    "description": "One typed input in a playground. `name` is the stable key in the submitted value map and in the reflected CSS property and data attribute. `label` names the generated native control. Every kind declares its initial value as a string: a finite number for range, `true` or `false` for toggle, one child value for choice, a six-digit hex colour for color, and arbitrary text for text. Range controls must add max and may add min, step, and unit. Choice controls contain one or more lf-playground-choice children; other kinds are empty.",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9-]*$"
      },
      "label": {
        "type": "string",
        "minLength": 1
      },
      "kind": {
        "enum": ["range", "toggle", "choice", "color", "text"]
      },
      "value": {
        "type": "string"
      },
      "min": {
        "type": "string",
        "pattern": "^-?(?:0|[1-9][0-9]*)(?:\\.[0-9]+)?$"
      },
      "max": {
        "type": "string",
        "pattern": "^-?(?:0|[1-9][0-9]*)(?:\\.[0-9]+)?$"
      },
      "step": {
        "type": "string",
        "pattern": "^(?:0\\.[0-9]*[1-9][0-9]*|[1-9][0-9]*(?:\\.[0-9]+)?)$"
      },
      "unit": {
        "type": "string"
      },
      "placeholder": {
        "type": "string"
      }
    },
    "required": ["name", "label", "kind", "value"],
    "allOf": [
      {
        "if": {
          "properties": {
            "kind": {
              "const": "range"
            }
          },
          "required": ["kind"]
        },
        "then": {
          "required": ["max"]
        }
      }
    ],
    "additionalProperties": false,
    "x-parent": ["lf-playground"],
    "x-content": "items",
    "x-upgrade": false
  },
  "lf-playground-choice": {
    "description": "One labeled value in a choice control. The value is stored and reflected; the label is the control text shown to the reader.",
    "type": "object",
    "properties": {
      "value": {
        "type": "string",
        "minLength": 1
      },
      "label": {
        "type": "string",
        "minLength": 1
      }
    },
    "required": ["value", "label"],
    "additionalProperties": false,
    "x-parent": ["lf-playground-control"],
    "x-content": "none",
    "x-upgrade": false
  },
  "lf-playground-preset": {
    "description": "One named partial configuration. Activating the generated preset button applies each child setting and leaves controls not named by the preset unchanged. The button remains visibly selected while all of its settings match the current values.",
    "type": "object",
    "properties": {
      "label": {
        "type": "string",
        "minLength": 1
      }
    },
    "required": ["label"],
    "additionalProperties": false,
    "x-parent": ["lf-playground"],
    "x-content": "items",
    "x-upgrade": false
  },
  "lf-playground-setting": {
    "description": "One value applied by a playground preset. `for` names a control in the same playground and `value` uses that control's authored string form.",
    "type": "object",
    "properties": {
      "for": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9-]*$"
      },
      "value": {
        "type": "string"
      }
    },
    "required": ["for", "value"],
    "additionalProperties": false,
    "x-parent": ["lf-playground-preset"],
    "x-content": "none",
    "x-upgrade": false
  },
  "lf-playground-preview": {
    "description": "The real, operable artifact a playground previews. An A/B comparison keeps both candidates here and drives them from one shared input state. Page CSS may bind reflected values from the enclosing lf-playground. An inline page module may consume the playground's public values and change event, own gestures and computed state, and load vendored or page-media assets from the page's origin.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "additionalProperties": false,
    "x-parent": ["lf-playground"],
    "x-content": "prose",
    "x-upgrade": false
  },
  "lf-playground-output": {
    "description": "The natural-language instruction produced by a playground. Write a self-contained task rather than a value dump, and place lf-playground-value elements where selected values make the task more precise. The choose action separately sends the complete typed value map. The package keeps this output live and offers a copy button.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "additionalProperties": false,
    "x-parent": ["lf-playground"],
    "x-content": "prose",
    "x-upgrade": false
  },
  "lf-playground-value": {
    "description": "One live value inside a playground instruction. `for` names a control in the enclosing playground. Numeric values include the control's unit.",
    "type": "object",
    "properties": {
      "for": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9-]*$"
      }
    },
    "required": ["for"],
    "additionalProperties": false,
    "x-parent": ["lf-playground-output"],
    "x-content": "none",
    "x-inline": true,
    "x-upgrade": false
  }
}
