bot_name: leaf-agent
harness: codex
model: gpt-6-sol
merge: yolo
control_plane_owner: "@max-sixty"

workflows:
  ci-fix:
    watched_workflows:
      - ci
      # A red `publish-site` leaves <https://leaf.page/> stale on whatever it
      # last built, which is invisible from the repo — worth a fix PR.
      - publish-site
