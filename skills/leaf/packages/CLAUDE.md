# Packages

`../references/packages.md` owns the package contract: layout, registry entries,
theme rules, guidance, data contracts, and requests. A widget module under any
`widgets/` directory follows the module contract in `../assets/CLAUDE.md`
("Module contract"), and `vendor/` bundles are generated by `scripts/vendor.py`
(`scripts/CLAUDE.md`).
