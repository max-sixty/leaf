/* One monitoring zone uses the shared reading-region lifecycle; its zone attribute
   belongs only to the package grid's placement. */
import { defineReadingPaneElement } from "/runtime/widget-api.js";

defineReadingPaneElement("lf-monitor-region");
