{
  "name": "leaf-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd .. && uv run scripts/site.py",
    "dev": "npm run build && wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7",
    "@openai/codex": "0.153.4",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260905.1",
    "@types/node": "^26.4.1",
    "esbuild": "0.28.1",
    "smol-toml": "1.5.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0",
    "wrangler": "^4.130.0"
  }
}
