{
  "$data": {
    "contracts": {
      "measured-scalar": {
        "description": "The latest scalar emitted by a repeatable measurement source. Authored lf-num text freezes the value a version states; the source snapshot records whether that measurement ran again.",
        "schema": {
          "type": ["number", "string"]
        }
      }
    }
  },
  "lf-ask": {
    "description": "One complete request to the reader: the heading, context, evidence, and answer control they need to consider it as a unit. It wraps exactly one non-quoted widget whose entry declares x-awaits; that nested widget still owns the action and determines whether the request is open. Use this when the request begins before its actionable widget — most often a section's heading and introductory prose above an lf-options group — so the Asks tray names the whole region and `n`/`p` arrives at its opening rather than dropping the reader straight into the answers. Keep the ordinary semantic section around it when the surrounding document needs a section landmark. The wrapper adds no frame or caption of its own.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-content": "prose",
    "x-ask": true,
    "x-upgrade": false,
    "x-example": "<section id=\"storage-decision\">\n  <lf-ask id=\"storage-ask\">\n    <h2>What a full store does</h2>\n    <p>The beta never reached the cap. This decides what the first workspace at it sees.</p>\n    <lf-options id=\"full-store\" choose label=\"What should a full store do?\">\n      <lf-option id=\"store-evict\"><strong>Drop the oldest documents</strong> Editing continues.</lf-option>\n      <lf-option id=\"store-stop\"><strong>Pause offline editing</strong> Nothing leaves.</lf-option>\n    </lf-options>\n  </lf-ask>\n</section>"
  },
  "lf-gloss": {
    "description": "A short, plain-text explanation attached to a phrase: hover the phrase to glance at it, focus its raised mark to reach it by keyboard, or click either to keep it open; Escape and a click elsewhere close it. Use one for a local term, acronym, or premise whose meaning a reader may want without leaving the sentence — not for a citation, a qualification the argument depends on, or an aside long enough to deserve visible prose, a details disclosure, or a sidenote. The phrase and `tip` are both the page's words: the phrase stays in its sentence, and the explanation can be selected, quoted, and commented on while open. A static copy or printout puts the explanation after the phrase as a quiet parenthetical note, so no authored meaning depends on hover or script. Keep the explanation self-contained and write it at the first useful occurrence rather than marking every repetition.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "tip": {
        "type": "string",
        "minLength": 1
      }
    },
    "required": [
      "tip"
    ],
    "additionalProperties": false,
    "x-content": "prose",
    "x-inline": true,
    "x-says": {
      "tip": "after"
    },
    "x-upgrade": true,
    "x-verbatim": true,
    "x-example": "Deploy through a <lf-gloss tip=\"A thin, end-to-end path through the real system.\">walking skeleton</lf-gloss> before parallelizing."
  },
  "lf-num": {
    "description": "One measured scalar frozen into the sentence that states it. Write the displayed value, unit included, as the element text; bind `source` to the repeatable measurement feed and set `at` to that source snapshot's recorded `updated` instant. A later `leaf data set` on the same source makes `version check` advise that this number is stale without replacing what an older version says. `via` may preserve the human-readable command or query in markup when the source id alone is not enough provenance. Use lf-metric instead when the number is a headline KPI in a metric row, and ordinary text for a number that is not produced by a repeatable measurement.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "source": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9-]*$"
      },
      "at": {
        "type": "string",
        "format": "date-time"
      },
      "via": {
        "type": "string",
        "minLength": 1
      }
    },
    "required": ["source", "at"],
    "additionalProperties": false,
    "x-content": "prose",
    "x-data": {
      "measurement": {
        "contract": "measured-scalar",
        "source": "source"
      }
    },
    "x-inline": true,
    "x-measured": {
      "input": "measurement",
      "at": "at"
    },
    "x-upgrade": false,
    "x-example": "The import now takes <lf-num source=\"import-latency\" at=\"2026-08-27T09:00:00Z\" via=\"uv run bench-import\">184 ms</lf-num> at p95."
  },
  "lf-toc": {
    "description": "A compact table of contents derived from the page's h2-h6 headings. Add one near the opening of a page whose sections are long enough that a reader benefits from seeing the route before reading it; short pages and pages with only one or two sections need no contents block. The runtime lists the headings in document order and indents deeper levels, so the headings remain the one authored outline. Existing heading ids become link targets. For an id-less heading, the widget adds a position-derived target beside it without changing the authored heading. Live links reveal headings inside disclosures or tabs, a copied link returns to the same version's section, and a standalone export keeps the same ordinary fragment links. A stable authored heading id keeps that target across versions. The generated labels are link apparatus rather than a second commentable copy of each heading, and version diffs ignore them as generated navigation. Write the element empty and give it a stable id.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "none",
    "x-upgrade": true,
    "x-example": "<h1>Migration plan</h1>\n<lf-toc id=\"plan-contents\"></lf-toc>\n<section><h2>Prepare</h2><p>Take a snapshot.</p><h3>Check capacity</h3><p>Leave headroom for the copy.</p></section>\n<section><h2>Move</h2><p>Shift one cohort at a time.</p></section>"
  },
  "lf-options": {
    "description": "A question the reader answers, and the alternatives it is about. `label` is that question, written on the group: the runtime renders it as the group's own first words (x-says), so the question is part of what the group says rather than a heading standing beside it. That is what lets anything reading the page's open asks name this one — a list of them has only the ask's own words to go on, and a group that holds no part of its question reads as its first option's chip band. It is also what lets a reader quote the question the way they quote an answer. Write it wherever the group would have a heading over it, and drop that heading rather than keeping a second copy of the question beside the thing that asks it. When the premise or evidence begins above the group, wrap that broader reading and this one group in lf-ask; the group keeps owning the choice while navigation arrives at the wrapper's opening. Children are lf-option elements only, and the form follows what they hold: an option leading with a <strong> title is a full-width card, and the cards stack — read down the page like a decision record, whether an option's case is one sentence or paragraphs with a `<dl class=\"facts\">` rail — so a decision with real argument keeps its comparison, its detail and its evidence inside the group rather than growing a table or an 'in detail' section beside it, and the group holds one shape at three options or six; a group whose options are bare labels renders as compact rows, for a question whose alternatives are sections of the page rather than cards (each row's `for` names one). Nothing declares which — write the options and the look follows. With `choose` (which requires an id), the reader picks — by click, or by keyboard via the toggle mark each option carries, which is also where its state reads. The group itself then draws as one control in every form, cells sharing hairlines rather than options standing apart — the question is the first of those cells, holding its words off the frame and opening at the column the options open at — and that shape is what says a decision is waiting: the mark says nothing until there is a pick to state, so no option captions itself with an offer. Clicking a picked option again clears it. `multiple` (which requires `choose`) makes the pick a set rather than one option, for \"which of these\" rather than \"which one\". The marks say which of the two it is, in the pair every reader already has: a round mark takes one, a square mark takes any, and under `multiple` an option not yet picked draws its empty box in every form, since an untaken slot is that option's own state rather than the group's offer repeated. A shape reaches no screen reader, so a mark says the same thing in words there, reading \"choose one\" or \"choose any\". Write the question and leave the arity to the marks; a page that spells out how many picks the control will take is captioning it. On the page the group's last cell is an option the reader writes: a plain box saying \"Another option\", drawn as a cell like the ones above it, for the answer the menu hasn't got — \"none of these — do X instead\", or the terms a pick comes with. So do not write a catch-all option of your own: the cell is already that option, and it takes the reader's words where a bare \"something else\" would take only their click. Words sent from it start a version-response comment thread anchored on the group, which then stands textually in that same cell and keeps its complete interactive view in Comments. In a thread (the group as a reply's or comment's markup — the AskUserQuestion shape) the thread's own reply box is already where another option goes, so the group adds none of its own; a `multiple` group there carries a Done press instead, whose `answer` action states the set is whole — every toggle reaches the agent as it lands, so the press is the one thing a live channel can't derive, and the ask stands until it (see until in $awaits). The keyboard answers a group wherever it stands: Ask navigation or Tab reaches a mark; ↑/↓ walk the options; 1–9 toggle the nth outright; Space toggles the focused option; and Enter reaches the box for another option while the page group still owns its unstarted box, or while its surrounding thread owns a reply box in Comments. Each option wears its digit while a mark holds focus. The pick reaches the agent as a `choose` action naming every picked option, empty once cleared, and carries onto every later version by itself — marking the picked options `chosen` in the next version is how the record reads without the log, not what keeps the pick alive. A pick rests on the options picked, so rewriting what one of them says needs its `restated` (see $restated in `page catalog`); a pick the reader has cleared rests on the group, and takes the group's. `settled` (also requiring an id) retires a decision that has been made and acted on: the group collapses to one line naming the chosen option, with the question still above it and every option — the reader's own cell among them — behind a disclosure the reader can open — the record keeps its ids and its anchors, and stops spending the page's height. Mark it once the choice has stopped being live, which is usually a version or two after honoring the pick, not the moment it was made; a settled group still takes a pick if the reader reopens it. A `choose` group is one of the page's standing asks until a pick rests on it, or the reader writes in its last cell and nobody has answered them yet (see $awaits): the banner counts it and `n`/`p` step the reader to it, and `settled` retires the ask with the decision. Writing an option is the reader dealing with the question, so the group stops asking — the ball is yours, and the page says so rather than sending them back to it. Nothing is recorded by it, though: the group still holds no pick and its controls still take one. Treat the reader's words as the answer by default: the next version adds them as a new lf-option, marks that option `chosen`, and then resolves the thread. `leaf reply` refuses this thread. If the revision needs clarification, open a separate exact-section thread on this Ask with `leaf comment --section <ask-id>` and leave the proposal in place while that question is answered. Nothing has to be marked `restated`, since words are not an action to retract.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "label": {
        "type": "string"
      },
      "choose": {
        "type": "boolean"
      },
      "multiple": {
        "type": "boolean"
      },
      "settled": {
        "type": "boolean"
      },
      "restated": {
        "type": "boolean"
      }
    },
    "dependentRequired": {
      "choose": [
        "id"
      ],
      "settled": [
        "id"
      ],
      "multiple": [
        "choose"
      ]
    },
    "additionalProperties": false,
    "x-content": "items",
    "x-says": {
      "label": "before"
    },
    "x-word": "module",
    "x-upgrade": true,
    "x-conversation": {
      "when": {
        "choose": [
          true
        ]
      },
      "response": {
        "kind": "version",
        "verb": "choose"
      }
    },
    "x-work": {
      "seat": "conversation",
      "when": {
        "settled": [
          false
        ]
      }
    },
    "x-awaits": {
      "when": {
        "choose": [
          true
        ],
        "settled": [
          false
        ]
      },
      "answers": [
        "choose"
      ],
      "until": {
        "verb": "answer",
        "when": {
          "multiple": [
            true
          ]
        }
      }
    },
    "x-state": {
      "choose": {
        "detail": {
          "type": "object",
          "properties": {
            "options": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "options"
          ],
          "additionalProperties": false
        },
        "facet": "selection",
        "unit": "widget",
        "record": {
          "kind": "attribute",
          "attr": "chosen",
          "value": "options"
        }
      },
      "answer": {
        "detail": {
          "type": "object",
          "additionalProperties": false
        },
        "facet": "completion",
        "unit": "widget"
      }
    },
    "x-example": "<lf-options id=\"run-status\" choose>\n  <lf-option id=\"rs-column\" recommended>\n    <lf-chip>effort: low</lf-chip><lf-chip tone=\"warn\">risk: med</lf-chip>\n    <strong>Status on the run list</strong>\n    <dl class=\"facts\"><dt>Cost</dt><dd>one join</dd><dt>Rollout</dt><dd>behind a flag</dd></dl>\n    <p>A failure reads off the list instead of costing a click each.</p>\n    <lf-shot id=\"rs-shot\" alt=\"the run list, before and after the status column\" before=\"/media/0f2c91a4b7de3105.png\" after=\"/media/6b40ac7719ee5d82.png\"></lf-shot>\n  </lf-option>\n  <lf-option id=\"rs-leave\">\n    <lf-chip>effort: none</lf-chip><lf-chip tone=\"ok\">risk: low</lf-chip>\n    <strong>Leave the list alone</strong>\n    <dl class=\"facts\"><dt>Cost</dt><dd>none</dd><dt>Rollout</dt><dd>nothing to roll</dd></dl>\n    <p>A failure stays one click away, and the list keeps the width it has.</p>\n  </lf-option>\n</lf-options>\n\n<p>Two of these are worth doing first — pick the ones to start with.</p>\n<lf-options id=\"first-jobs\" choose multiple>\n  <lf-option id=\"job-mounts\" for=\"sec-mounts\">Replace the mounts</lf-option>\n  <lf-option id=\"job-heater\" for=\"sec-heater\">Heat the bird bath</lf-option>\n  <lf-option id=\"job-camera\">Neither — the camera first</lf-option>\n</lf-options>"
  },
  "lf-option": {
    "description": "One alternative in a decision, in either of the group's two forms: lead with a <strong> title and the option is a full-width card on a row of its own, whose case can be a sentence of prose or real block content — paragraphs, valence-tagged bullets ('Good, because …'), a `<dl class=\"facts\">` straight after the title, which docks right as a rail at one fixed width so every option's scalars align down the group, and the evidence the decision turns on: a lf-shot's before/after pair, a diagram, a figure, a widget. Put that evidence in the option it decides rather than in a section beside the group — a reader who looks in one place and presses in another is comparing from memory. Size it for the card and not for the page: an image takes whatever width it is given, a mermaid diagram takes the width its nodes need and scrolls inside the card for the rest, so a diagram written for an option is a short one, and a rail docked beside it costs 10rem of the card besides. Working that evidence is never a pick: flipping a shot, opening a `<details>`, walking a nested widget's own controls all leave the question open, and the option's own words, its chips and its mark are what answer it. Or write a short label alone, which is a question row. `for` is the id of the block on the page this option is about — the section it would change, the work it names — and renders as a reference the reader can follow. The label stays written here either way; `for` points, it never supplies words. `recommended` draws the accent ring (the author's take); `chosen` the green one (the reader's pick — the choose upgrade sets it live, and the next version carries it so the page reads right without the log). Chips are `lf-chip` children written before the title — the band a scanning eye reads first. Rewriting what a picked option says needs `restated` on that option (see $restated in `page catalog`); the other options in the group are free to change.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "for": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "recommended": {
        "type": "boolean"
      },
      "chosen": {
        "type": "boolean"
      },
      "restated": {
        "type": "boolean"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-parent": [
      "lf-options"
    ],
    "x-content": "prose",
    "x-refers": [
      "for"
    ],
    "x-paints": [
      "recommended"
    ],
    "x-upgrade": false
  },
  "lf-chip": {
    "description": "One label in the band above an option's or a variant's title: what the decision turns on, in the words that fit it. `effort: low` and `risk: high` are worth starting from, since most decisions do turn on those two, but they are a suggestion and nothing more — `blast-radius: high`, `backward compat: no`, `cost: £240`, `migration: two tables` are the same slot, and a chip is as free in its answer as in its name: a scale of your own, a yes or a no, a quantity, a few words. Reach for the ones the options actually differ on, write them before the <strong> title (a lf-variant takes them the same way, being the same shape without the decision), and write the same ones on every option in the group, in the same order, so they read down it. They are the glance and not the comparison — two or three an eye takes in before it reads, where a `<dl class=\"facts\">` rail is the dimensions the options are argued on at whatever length that takes. An option can carry both, and only the rail costs a card its terseness: a `<dl>` is block content, so it stacks the group into rows, and chips never do. `tone` tints one chip — the layer's own vocabulary, `$tones.names`, which `page catalog` prints — and is the author's judgement about this answer, not a property of the facet: `risk: high` is red because you say so, and the same word on another page may not be. Leave it off and the chip is neutral, which is what most of them should be; colour that lands on every chip has stopped pointing at anything. It is the same bargain `lf-metric` makes with `direction` — the theme knows the tints, the author knows which way is good.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "tone": {
        "type": "string"
      }
    },
    "additionalProperties": false,
    "x-parent": [
      "lf-option",
      "lf-variant"
    ],
    "x-content": "prose",
    "x-inline": true,
    "x-tone": "tone",
    "x-upgrade": false
  },
  "lf-milestones": {
    "description": "A timeline of milestones with status dots on a rail. Children are lf-milestone elements only.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": false,
    "x-example": "<lf-milestones>\n  <lf-milestone id=\"m-survey\" status=\"done\" when=\"week 1\">\n    <strong>Survey the sites</strong> Map sun and squirrel routes.\n  </lf-milestone>\n  <lf-milestone id=\"m-build\" status=\"active\" when=\"weeks 2-3\" tags=\"wood,solar\">\n    <strong>Build the feeders</strong> Two classic, two heated.\n  </lf-milestone>\n  <lf-milestone id=\"m-install\" status=\"planned\" when=\"week 4\">\n    <strong>Install and watch</strong>\n  </lf-milestone>\n</lf-milestones>"
  },
  "lf-milestone": {
    "description": "One milestone. Lead with a <strong> title; prose follows. `status` colors the timeline dot; `when` is a free-text schedule label; `tags` is a comma-separated list rendered as chips.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "status": {
        "enum": [
          "planned",
          "active",
          "done",
          "blocked"
        ]
      },
      "when": {
        "type": "string"
      },
      "tags": {
        "type": "string",
        "pattern": "^[a-z0-9-]+(,[a-z0-9-]+)*$"
      }
    },
    "required": [
      "id",
      "status"
    ],
    "additionalProperties": false,
    "x-parent": [
      "lf-milestones"
    ],
    "x-content": "prose",
    "x-paints": [
      "status"
    ],
    "x-upgrade": true
  },
  "lf-timeline": {
    "description": "A chronological event log — the incident-report shape. Children are lf-event elements only. Where lf-milestones tracks planned work by status, lf-timeline records what happened, when.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": false,
    "x-example": "<lf-timeline>\n  <lf-event id=\"e-alert\" at=\"09:14\" kind=\"warning\">\n    <strong>Feeder camera offline</strong> Battery alert from the north feeder.\n  </lf-event>\n  <lf-event id=\"e-swap\" at=\"09:40\" kind=\"action\">\n    <strong>Battery swapped</strong>\n  </lf-event>\n  <lf-event id=\"e-ok\" at=\"09:41\" kind=\"info\">\n    <strong>Camera back</strong> Feed resumed.\n  </lf-event>\n</lf-timeline>"
  },
  "lf-event": {
    "description": "One event on a timeline. Lead with a <strong> title; prose follows. `at` is a free-text time label; `kind` colors the dot.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "at": {
        "type": "string"
      },
      "kind": {
        "enum": [
          "info",
          "action",
          "warning",
          "failure"
        ]
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-parent": [
      "lf-timeline"
    ],
    "x-content": "prose",
    "x-says": {
      "at": "before"
    },
    "x-paints": [
      "kind"
    ],
    "x-upgrade": false
  },
  "lf-compare": {
    "description": "Labeled variants — neutral exhibition, where lf-options is a decision. Children are lf-variant elements only. Layout follows lf-options: terse variants sit side by side, block content in any variant stacks the group into rows, and a `<dl class=\"facts\">` after the label docks as an aligned rail.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": false,
    "x-example": "<lf-compare>\n  <lf-variant id=\"v-cedar\"><strong>Cedar</strong> Weathers silver; no sealant.</lf-variant>\n  <lf-variant id=\"v-pine\"><strong>Pine</strong> Cheaper; seal it yearly.</lf-variant>\n</lf-compare>"
  },
  "lf-variant": {
    "description": "One variant in a comparison. Lead with a <strong> label; any content follows, widgets included.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-parent": [
      "lf-compare"
    ],
    "x-content": "prose",
    "x-upgrade": false
  },
  "lf-metrics": {
    "description": "A KPI row. Children are lf-metric elements only.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": false,
    "x-example": "<lf-metrics>\n  <lf-metric id=\"k-visits\" value=\"312\" delta=\"+41\" direction=\"up-good\">daily visits</lf-metric>\n  <lf-metric id=\"k-refills\" value=\"2.1/wk\" delta=\"+0.4\" direction=\"up-bad\">refills</lf-metric>\n  <lf-metric id=\"k-species\" value=\"14\">species seen</lf-metric>\n</lf-metrics>"
  },
  "lf-metric": {
    "description": "One metric: element text is the label, `value` the headline number, `delta` a signed change (leading + or -), `direction` says which sign is good (up-good, up-bad) so the delta colors honestly; without it the delta stays neutral.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "value": {
        "type": "string"
      },
      "delta": {
        "type": "string",
        "pattern": "^[+-].+$"
      },
      "direction": {
        "enum": [
          "up-good",
          "up-bad"
        ]
      }
    },
    "required": [
      "id",
      "value"
    ],
    "additionalProperties": false,
    "x-parent": [
      "lf-metrics"
    ],
    "x-content": "prose",
    "x-says": {
      "value": "before",
      "delta": "after"
    },
    "x-upgrade": false
  },
  "lf-diff": {
    "description": "A unified diff rendered by Pierre with native per-file collapse, full add/delete line backgrounds, brighter inline word changes, visible but unselectable +/− indicators, and per-file +/− counts. Selecting a source line therefore copies only its code. The body is data: the diff verbatim inside its <pre>, at column 0. Every changed file needs at least one `@@` hunk header, whose old and new counts must match the following lines exactly. A path-only rename is the only supported hunkless entry and appears as one old → new row; write that block as exactly four lines: `diff --git`, `similarity index 100%`, `rename from`, and `rename to`, with the same non-empty old and new paths in the header and rename lines. A rename with changes uses the normal disclosure labeled with its new path. Binary, mode-only, empty added/deleted, and copy entries are unsupported, so state those changes in prose rather than mixing them into the diff; for an edited copy destination, omit the copy metadata and provide its textual hunks. Context lines keep their leading space; encode a context line with nothing after that prefix as &amp;#32; so HTML parsing preserves it. HTML-escape the body — & as &amp;amp; first, then < and > as &amp;lt; and &amp;gt; — or a diff whose lines carry entity text (&amp;amp; in source, say) is silently altered before rendering. Comments anchor to the required id. Takes no language: each file's own path is read for one ($languages), so keep the `diff --git` or ---/+++ headers rather than pasting bare hunks, and a path naming no language leaves that file plain.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "data",
    "x-upgrade": true,
    "x-shadow": true,
    "x-example": "<lf-diff id=\"mount-diff\"><pre>\ndiff --git a/feeders/mount.py b/feeders/mount.py\n--- a/feeders/mount.py\n+++ b/feeders/mount.py\n@@ -1,2 +1,2 @@\n def bracket():\n-    return \"plastic\"\n+    return \"steel\"\n</pre></lf-diff>"
  },
  "lf-tree": {
    "description": "A file tree from an indent-notation body: one node per line, children indented deeper, a trailing / marking a directory, trailing +N/-N tokens rendering as change badges. Body is data — text inside its <pre>, HTML-escaped (& first, then < and >).",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "data",
    "x-upgrade": true,
    "x-example": "<lf-tree id=\"feeder-tree\"><pre>\nfeeders/\n  mount.py  +2 -2\n  schedule.py  +1\n  sites/\n    north.toml\n</pre></lf-tree>"
  },
  "lf-code": {
    "description": "An annotated code block — the walkthrough shape. The body is the code verbatim inside its <pre> (data, HTML-escaped — & first, then < and > — column 0); `language` colors it, naming one of $languages; `hi` highlights line ranges (\"3-5,8\"); lf-note children sit beside that <pre> and anchor remarks at lines by number. Leave `language` off where the body isn't source (a transcript, command output) — nothing guesses, so an undeclared block stays plain.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "language": {
        "type": "string"
      },
      "hi": {
        "type": "string",
        "pattern": "^[0-9]+(-[0-9]+)?(,[0-9]+(-[0-9]+)?)*$"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "data",
    "x-language": "language",
    "x-lines": [
      "hi"
    ],
    "x-upgrade": true,
    "x-example": "<lf-code id=\"bracket-walk\" language=\"python\" hi=\"2\"><pre>\ndef bracket(temp):\n    if temp &lt; 0:\n        return \"steel\"\n    return \"cedar\"\n</pre>\n  <lf-note at=\"2\">Freezing is the only threshold that matters.</lf-note>\n</lf-code>"
  },
  "lf-note": {
    "description": "A remark anchored at a line of its lf-code parent. Prose content; `at` is the 1-based line number.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "at": {
        "type": "string",
        "pattern": "^[0-9]+$"
      }
    },
    "required": [
      "at"
    ],
    "additionalProperties": false,
    "x-parent": [
      "lf-code"
    ],
    "x-content": "prose",
    "x-lines": [
      "at"
    ],
    "x-upgrade": false
  },
  "lf-diagram": {
    "description": "A diagram rendered from a mermaid-source body (flowchart, sequence, state…). The body is data, not prose: text inside its <pre>, HTML-escaped (& first, then the angle brackets). A click comments on the whole diagram by default. Flowchart nodes listed in `parts` as `node:SOURCE_ID` take individual comments; the source id is the stable id written before a node's label in Mermaid. Unlisted nodes and diagrams authored without `parts` keep the whole-diagram behavior. `parts` is a flowchart-only contract: declaring it on another Mermaid type is a render error rather than a promise that cannot resolve.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "parts": {
        "type": "string",
        "minLength": 1,
        "pattern": "^node:[A-Za-z_][A-Za-z0-9_-]*(?:\\s+node:[A-Za-z_][A-Za-z0-9_-]*)*$"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "data",
    "x-upgrade": true,
    "x-visual": {
      "parts": "parts"
    },
    "x-wide": "drawing",
    "x-example": "<lf-diagram id=\"checkout-flow\" parts=\"node:Cart node:Pay node:Receipt\"><pre>\ngraph LR\n  Cart --> Pay\n  Pay --> Receipt\n</pre></lf-diagram>"
  },
  "lf-chart": {
    "description": "A chart drawn from a comma-separated body: the header row names the x column and then one column per series, and every row after it is one x value and its numbers (blank where a series has none). The body is data, not prose: text inside its <pre>, HTML-escaped (& first, then the angle brackets). `kind` says which picture — bars (a group of bars per x value), rows (horizontal bars, for long names or a ranking), stack (bars stacked into a total, so no negatives), line (a run over time or an ordered x), dots (two numbers against each other, x column numeric). `y` names what the numbers are. A bar's x is one slot per row whatever it says; a line or a scatter reads its column, so dates (2026-06-01, or 2026-06 for a month) get a time axis and numbers a numeric one. Two rows may not share an x value, and a series column may not be empty. At most $series.steps series — fold the small ones together or split the chart. There is no pie, because the comparison it asks for is between angles and the comparison bars ask for is between lengths. Comments anchor to the required id, not to text inside the drawing.",

    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "kind": {
        "enum": [
          "bars",
          "rows",
          "stack",
          "line",
          "dots"
        ]
      },
      "y": {
        "type": "string",
        "minLength": 1
      }
    },
    "required": [
      "id",
      "kind",
      "y"
    ],
    "additionalProperties": false,
    "x-content": "data",
    "x-upgrade": true,
    "x-visual": "whole",
    "x-example": "<lf-chart id=\"review-latency\" kind=\"line\" y=\"hours to first review\"><pre>\nweek, backend, frontend\n2026-05-04, 31, 18\n2026-05-11, 26, 21\n2026-05-18, 19, 14\n2026-05-25, 12, 11\n</pre></lf-chart>"
  },
  "lf-shot": {
    "description": "A before/after pair of screenshots of one view, shown one at a time in a single frame: the reader clicks the image itself to swap the two, and the change is the only thing on screen that moves. The frame is the target rather than a control beside it, so the pointer rests on the change and alternates without re-aiming — which is the whole of what a flip is worth, and what a switch under the image spends. A switch sits there anyway for the keyboard, and the caption in the corner says which state is up. `before` and `after` are served image paths — `leaf page media <page> <file>…` copies files into the page directory and prints the path to write, which is the only way an image gets onto a page (a screenshot is far too many bytes to inline). `alt` says what the pair shows, for a reader who can't see it. The element takes no content: what the page has to say about the change is ordinary prose written around it, where the user can quote it. Where the change is what a decision turns on, the pair goes inside the lf-option proposing it, so the reader flips it and presses in one place. Reach for it when the change reads at page scale — layout, spacing, a component swapped out. Below that scale the column's width is against you: crop the pair to the part that moved, or show the element itself before and after at real size.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "before": {
        "type": "string",
        "pattern": "^/media/\\S+$"
      },
      "after": {
        "type": "string",
        "pattern": "^/media/\\S+$"
      },
      "alt": {
        "type": "string"
      }
    },
    "required": [
      "id",
      "before",
      "after",
      "alt"
    ],
    "additionalProperties": false,
    "x-content": "none",
    "x-upgrade": true,
    "x-example": "<h3>The option cards</h3>\n<p>The chips now sit above the title, so a scanning eye reads cost before prose.</p>\n<lf-shot id=\"shot-cards\" alt=\"the decision page's option cards\" before=\"/media/0f2c91a4b7de3105.png\" after=\"/media/6b40ac7719ee5d82.png\"></lf-shot>"
  },
  "lf-board": {
    "description": "A kanban board the user edits directly: cards move between columns or reorder within one — dragged by the grip, or by keyboard (the grip is a button: Enter grabs, arrows move, Enter drops) — and each completed move reaches the agent as a `move` action naming the card, target column, and position. Children are lf-column elements only. Write the board as you mean it and leave it alone afterwards: a recorded move carries onto every later version by itself, so re-emitting a card where you first put it does not undo the user's move. Replay preserves the exact position, but the pending mark, version diff, and restatement gate compare a card's column only: a reorder within one column is deliberately not called out as a separate state change, because raw indexes do not compose when neighbouring cards move. A move rests on its card, so revising one they have moved is the card's `restated` (see $restated in `page catalog`), never the board's.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": true,
    "x-state": {
      "move": {
        "detail": {
          "type": "object",
          "properties": {
            "card": {
              "type": "string"
            },
            "to": {
              "type": "string"
            },
            "index": {
              "type": "integer",
              "minimum": 0
            }
          },
          "required": [
            "card",
            "to",
            "index"
          ],
          "additionalProperties": false
        },
        "facet": "placement",
        "unit": "card",
        "record": {
          "kind": "position",
          "within": "lf-column",
          "value": "to",
          "order": "index"
        }
      }
    },
    "x-wide": "box",
    "x-example": "<lf-board id=\"feeder-board\">\n  <lf-column id=\"col-todo\" label=\"To do\">\n    <lf-card id=\"card-heater\"><strong>Heated perch</strong> Wire the south feeder.</lf-card>\n    <lf-card id=\"card-baffle\"><strong>Squirrel baffle</strong></lf-card>\n  </lf-column>\n  <lf-column id=\"col-doing\" label=\"Doing\">\n    <lf-card id=\"card-camera\"><strong>Camera mount</strong></lf-card>\n  </lf-column>\n  <lf-column id=\"col-done\" label=\"Done\"></lf-column>\n</lf-board>"
  },
  "lf-column": {
    "description": "One column on a board. `label` is the column heading, and the name a screen reader announces for the list of cards beneath it — so it has to read as the column's name on its own. Children are lf-card elements only; an empty column is a valid drop target.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "label": {
        "type": "string"
      }
    },
    "required": [
      "id",
      "label"
    ],
    "additionalProperties": false,
    "x-parent": [
      "lf-board"
    ],
    "x-content": "items",
    "x-upgrade": false,
    "x-says": {
      "label": "before"
    }
  },
  "lf-card": {
    "description": "One card on a board. Lead with a <strong> title; prose follows. The user drags it by its grip; the id names it in `move` actions and keeps comment anchors stable across moves. Rewriting a card the user has moved needs `restated` on the card (see $restated in `page catalog`), which retracts the moves of that card alone — the rest of the board keeps its arrangement.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "restated": {
        "type": "boolean"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-parent": [
      "lf-column"
    ],
    "x-content": "prose",
    "x-work": {"seat": "content"},
    "x-upgrade": false
  },
  "lf-tabs": {
    "description": "Workstreams on one page: use one family when broad context branches into focused work. Keep the shared title and lede before it, put the current stream first, and keep earlier context intact in a sibling panel so its stable ids retain their comments and decisions. Ordering gives the first panel emphasis and makes it the default for a reader with no remembered panel or reading position. A saved panel or restored position takes precedence because the open tab is the reader's view state — remembered per browser tab like scroll position, never an action, never carried by a version. Each lf-tab is a self-contained sub-context with its own prose and widgets, while Comments, Asks, versions, and sign-off remain global. Find-in-page, ask travel, and comment-anchor jumps open the owning tab. Children are lf-tab elements only; unupgraded, panels stack as labeled sections.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": true,
    "x-example": "<lf-tabs id=\"workstreams\">\n  <lf-tab id=\"tab-implementation\" label=\"Implementation\">\n    <p id=\"rollout-plan\">Wire the first site this week.</p>\n  </lf-tab>\n  <lf-tab id=\"tab-vision\" label=\"Vision\">\n    <p id=\"product-vision\">Keep the original outcome and constraints in reach.</p>\n  </lf-tab>\n</lf-tabs>"
  },
  "lf-tab": {
    "description": "One workstream panel. `label` names its tab in the strip (and the stacked heading before upgrade); content is open — prose, sections, and other widgets nest freely. Ids inside share the page's one universe, so keep them distinct across tabs.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "label": {
        "type": "string"
      }
    },
    "required": [
      "id",
      "label"
    ],
    "additionalProperties": false,
    "x-parent": [
      "lf-tabs"
    ],
    "x-content": "prose",
    "x-upgrade": false
  },
  "lf-draft": {
    "description": "A block of text the user owns: they double-click it (or its ✎ button) to rewrite it in place, and the exact words reach the agent as an `edit` action naming the element. After the first edit, Changes shows a bounded, lossless word-aligned difference from this version, every recorded body in order, and a Restore control for each older body; very large divergent middles appear as one replacement rather than freezing the page. Restoring posts another ordinary `edit`, so it converges across tabs and later versions instead of creating private undo state. The body is the draft text verbatim inside its <pre> (data, HTML-escaped — & first, then < and >; newlines preserved). It carries no decision chrome of its own — put a draft inside a lf-card where the column is the verdict, or let page sign-off approve the page as a whole. Usable anywhere prose flows. The agent rewriting prose of its own goes in a lf-suggestion instead; a draft is the user's own words, so there is nothing for them to accept. Once they have edited one those words are theirs: re-emit the body you wrote and their edit still stands over it — and `leaf comment` reads it their way, quoting the words their edit left and refusing the ones it replaced. To take the pen back — a fact of yours to fix in a sentence they rewrote — write the new text and mark the element `restated` (see $restated in `page catalog`).",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "needed": {
        "type": "boolean"
      },
      "restated": {
        "type": "boolean"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "data",
    "x-word": "module",
    "x-awaits": {
      "when": {
        "needed": [
          true
        ]
      },
      "answers": [
        "edit"
      ]
    },
    "x-upgrade": true,
    "x-verbatim": true,
    "x-state": {
      "edit": {
        "detail": {
          "type": "object",
          "properties": {
            "text": {
              "type": "string"
            }
          },
          "required": [
            "text"
          ],
          "additionalProperties": false
        },
        "facet": "body",
        "unit": "widget",
        "record": {
          "kind": "body",
          "value": "text"
        }
      }
    },
    "x-example": "<lf-draft id=\"note-cli\"><pre>\nAdds --dry-run to every mutating command.\n</pre></lf-draft>"
  },
  "lf-specimen": {
    "description": "Quoted material: a region the page exhibits rather than speaks — a sample decision, a demo board, markup shown as an example. Renders as quotation: a muted left gutter beside the quoted region, with a small-caps \"quoted\" line above it (`label` extends that line — \"quoted · a decision\"). Content is open and renders at full fidelity, but it is a mention, not a use: interactive widgets inside are inert — a choose group takes no clicks, a board no drags, a suggestion no decision. Keep the content visibly fictional, and let the gutter do the marking: a caption explaining that the region is an example is the page narrating a widget it could just use.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "label": {
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "prose",
    "x-says": {
      "label": "before"
    },
    "x-exhibit": true,
    "x-upgrade": false,
    "x-example": "<lf-specimen id=\"spec-gear\" label=\"next season's kit\">\n  <lf-options>\n    <lf-option id=\"sp-paper\"><lf-chip>effort: low</lf-chip><strong>Paper maps</strong> Nothing to charge.</lf-option>\n    <lf-option id=\"sp-gps\" recommended><lf-chip>effort: med</lf-chip><strong>Dedicated GPS</strong> Offline maps, week-long battery.</lf-option>\n  </lf-options>\n</lf-specimen>"
  },
  "$reactions": {
    "tokens": {
      "ok":   {"glyph": "✓", "means": "settled — no change asked", "settles": true},
      "no":   {"glyph": "×", "means": "this is wrong; the passage is the referent"},
      "lost": {"glyph": "?", "means": "unclear — rewrite it so it lands"},
      "cut":  {"glyph": "−", "means": "does not earn its length — shorten or drop"},
      "more": {"glyph": "+", "means": "expand here"},
      "this": {"glyph": "⌖", "means": "look at this — attention, no verdict"}
    }
  }
}
