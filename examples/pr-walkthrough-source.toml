# gateway/ratelimit.conf at review revision 8f61c2a
# Identity prefixes keep tokens and remote addresses disjoint in the shared bucket table.
[buckets]
identity = "token_or_remote_address"
burst_multiplier = 2
bucket_ttl_minutes = 30

[fallback]
untokened_per_minute = 60
prefix = "ip:"
