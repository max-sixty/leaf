Preview shots for pull request #129. Not part of the plugin tree — this branch exists only so the images render in the PR thread, and it can be deleted once the thread is read.

Fixture: `ADDRESSED_PAGE` with `comments=1` (foot shots) and `comments=8` (list shots), 420x900, light scheme, panel opened with `c`. Crops are the viewport's bottom 260px.
