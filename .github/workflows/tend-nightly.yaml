# Generated by tend 0.3.2. Regenerate with: uvx tend@latest init
#
# Do not edit this file directly — it will be overwritten on regeneration.
# To change this workflow, edit .config/tend.yaml. To change what the agent
# does, edit the relevant skill (for example, `running-tend`) in this repo's
# .claude/skills/ directory, or open an issue at
# https://github.com/max-sixty/tend/issues for changes that need to
# happen upstream in the tend-ci-runner plugin.

name: tend-nightly
on:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

jobs:
  nightly:
    if: github.repository_owner == 'max-sixty' && vars.TEND_ENABLED != 'false'
    runs-on: ubuntu-24.04
    environment:
      name: tend
      deployment: false
    permissions:
      contents: write
      pull-requests: write
      actions: read
      issues: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
          fetch-tags: true
          token: ${{ secrets.TEND_BOT_TOKEN }}

      - uses: astral-sh/setup-uv@v7
      - run: uv sync --frozen
      - run: uv run playwright install --with-deps chromium webkit --only-shell
        name: Install Chromium headless shell and WebKit
      - run: uv run python -c "from playwright.sync_api import sync_playwright; p = sync_playwright().start(); p.chromium.launch().close(); p.webkit.launch().close(); p.stop()"
        name: Launch both browsers
      - run: echo "CLOUDFLARE_API_TOKEN=$CLOUDFLARE_API_TOKEN" >> "$GITHUB_ENV"
        name: Export the Cloudflare log token for agents that read the logs
        env:
          CLOUDFLARE_API_TOKEN: ${{ ((github.event_name == 'workflow_run' && github.event.workflow_run.head_repository.full_name == github.repository) || (github.event.issue && !github.event.issue.pull_request)) && secrets.CLOUDFLARE_API_TOKEN || '' }}

      - uses: max-sixty/tend/codex@0.3.2
        with:
          github_token: ${{ secrets.TEND_BOT_TOKEN }}
          merge: restricted
          control_plane_owner: ""
          openai_api_key: ${{ secrets.OPENAI_API_KEY }}
          codex_auth_json: ${{ secrets.CODEX_AUTH_JSON }}
          bot_name: leaf-agent
          model: gpt-6-sol
          checkout_mode: base
          base_branch: ${{ github.event.repository.default_branch }}
          prompt: |2
            $nightly
