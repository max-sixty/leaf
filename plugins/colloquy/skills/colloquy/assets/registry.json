{
  "cq-options": {
    "description": "A question the reader answers, and the alternatives it is about. Children are cq-option elements only, and the form follows what they hold: an option leading with a <strong> title is a full-width card, and the cards stack — read down the page like a decision record, whether an option's case is one sentence or paragraphs with a `<dl class=\"facts\">` rail — so a decision with real argument keeps its comparison and its detail inside the group rather than growing a table or an 'in detail' section beside it, and the group holds one shape at three options or six; a group whose options are bare labels renders as compact rows, for a question whose alternatives are sections of the page rather than cards (each row's `for` names one). Nothing declares which — write the options and the look follows. With `choose` (which requires an id), the reader picks — by click, or by keyboard via the toggle mark each option carries, which is also where its state reads. The group itself then draws as one control in every form, cells sharing hairlines rather than options standing apart, and that shape is what says a decision is waiting: the mark says nothing until there is a pick to state, so no option captions itself with an offer. Clicking a picked option again clears it. `multiple` (which requires `choose`) makes the pick a set rather than one option, for \"which of these\" rather than \"which one\". The marks say which of the two it is, in the pair every reader already has: a round mark takes one, a square mark takes any, and under `multiple` an option not yet picked draws its empty box in every form, since an untaken slot is that option's own state rather than the group's offer repeated. A shape reaches no screen reader, so a mark says the same thing in words there, reading \"choose one\" or \"choose any\". Write the question and leave the arity to the marks; a page that spells out how many picks the control will take is captioning it. On the page a `choose` group also carries a box for words: what the reader types there reaches the agent as an ordinary comment anchored on the group, so an answer the options don't cover — \"none of these\", or a pick's why — needs no separate gesture. In a thread (the group as a reply's or comment's markup — the AskUserQuestion shape) the thread's own reply box is already that channel, so the group adds no box; a `multiple` group there carries a Done press instead, whose `answer` action states the set is whole — every toggle reaches the agent as it lands, so the press is the one thing a live channel can't derive, and the ask stands until it (see until in $awaits). The keyboard answers a group wherever it stands: Tab reaches a mark, ↑/↓ walk the options, ⏎ toggles, and 1–9 pick the nth outright — each option wears its digit while a mark holds focus. The pick reaches the agent as a `choose` action naming every picked option, empty once cleared, and carries onto every later version by itself — marking the picked options `chosen` in the next version is how the record reads without the log, not what keeps the pick alive. A pick rests on the options picked, so rewriting what one of them says needs its `restated` (see $restated in `page catalog`); a pick the reader has cleared rests on the group, and takes the group's. `settled` (also requiring an id) retires a decision that has been made and acted on: the group collapses to one line naming the chosen option, with every option behind a disclosure the reader can open — the record keeps its ids and its anchors, and stops spending the page's height. Mark it once the choice has stopped being live, which is usually a version or two after honoring the pick, not the moment it was made; a settled group still takes a pick if the reader reopens it. A `choose` group is one of the page's standing asks until a pick rests on it (see $awaits): the banner counts it and `a` steps the reader to it, and `settled` retires the ask with the decision.",
    "type": "object",
    "properties": {
      "id":       {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "choose":   {"type": "boolean"},
      "multiple": {"type": "boolean"},
      "settled":  {"type": "boolean"},
      "restated": {"type": "boolean"}
    },
    "dependentRequired": {"choose": ["id"], "settled": ["id"], "multiple": ["choose"]},
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": true,
    "x-awaits": {"when": {"choose": [true], "settled": [false]}, "until": {"verb": "answer", "when": {"multiple": [true]}}},
    "x-state": {
      "choose": {
        "detail": {"type": "object", "properties": {"options": {"type": "array", "items": {"type": "string"}}}, "required": ["options"], "additionalProperties": false},
        "unit": "widget",
        "record": {"kind": "attribute", "attr": "chosen", "value": "options"}
      },
      "answer": {
        "detail": {"type": "object", "additionalProperties": false},
        "unit": "widget"
      }
    },
    "x-example": "<cq-options id=\"nav-gear\" choose>\n  <cq-option id=\"paper-maps\">\n    <cq-chip>effort: low</cq-chip><cq-chip>risk: high</cq-chip>\n    <strong>Paper maps</strong>\n    <dl class=\"facts\"><dt>Cost</dt><dd>£12</dd><dt>Battery</dt><dd>none needed</dd></dl>\n    <p>Print the route ahead of time; nothing to charge, nothing to drop.</p>\n  </cq-option>\n  <cq-option id=\"gps-unit\" recommended>\n    <cq-chip>effort: med</cq-chip><cq-chip>risk: low</cq-chip>\n    <strong>Dedicated GPS</strong>\n    <dl class=\"facts\"><dt>Cost</dt><dd>£240</dd><dt>Battery</dt><dd>a week</dd></dl>\n    <p>Offline maps, and a reversible choice — resellable if the season ends early.</p>\n  </cq-option>\n</cq-options>\n\n<p>Two of these are worth doing first — pick the ones to start with.</p>\n<cq-options id=\"first-jobs\" choose multiple>\n  <cq-option id=\"job-mounts\" for=\"sec-mounts\">Replace the mounts</cq-option>\n  <cq-option id=\"job-heater\" for=\"sec-heater\">Heat the bird bath</cq-option>\n  <cq-option id=\"job-camera\">Neither — the camera first</cq-option>\n</cq-options>"
  },
  "cq-option": {
    "description": "One alternative in a decision, in either of the group's two forms: lead with a <strong> title and the option is a full-width card on a row of its own, whose case can be a sentence of prose or real block content — paragraphs, valence-tagged bullets ('Good, because …'), a `<dl class=\"facts\">` straight after the title, which docks right as a rail at one fixed width so every option's scalars align down the group; or write a short label alone, which is a question row. `for` is the id of the block on the page this option is about — the section, diagram or figure that makes its case — and renders as a reference the reader can follow, so the alternatives can be the page itself rather than what the cards can hold. The label stays written here either way; `for` points, it never supplies words. `recommended` draws the accent ring (the author's take); `chosen` the green one (the reader's pick — the choose upgrade sets it live, and the next version carries it so the page reads right without the log). Chips are `cq-chip` children written before the title — the band a scanning eye reads first. Rewriting what a picked option says needs `restated` on that option (see $restated in `page catalog`); the other options in the group are free to change.",
    "type": "object",
    "properties": {
      "id":          {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "for":         {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "recommended": {"type": "boolean"},
      "chosen":      {"type": "boolean"},
      "restated":    {"type": "boolean"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-parent": ["cq-options"],
    "x-content": "prose",
    "x-refers": ["for"],
    "x-upgrade": false
  },
  "cq-chip": {
    "description": "One label in the band above an option's or a variant's title: what the decision turns on, in the words that fit it. `effort: low` and `risk: high` are worth starting from, since most decisions do turn on those two, but they are a suggestion and nothing more — `blast-radius: high`, `backward compat: no`, `cost: £240`, `migration: two tables` are the same slot, and a chip is as free in its answer as in its name: a scale of your own, a yes or a no, a quantity, a few words. Reach for the ones the options actually differ on, write them before the <strong> title (a cq-variant takes them the same way, being the same shape without the decision), and write the same ones on every option in the group, in the same order, so they read down it. They are the glance and not the comparison — two or three an eye takes in before it reads, where a `<dl class=\"facts\">` rail is the dimensions the options are argued on at whatever length that takes. An option can carry both, and neither changes the group's shape: the cards stack whatever an option holds, chips in their band and the rail docked in the card's own row. (A variant is the one place content still decides: cq-compare sits terse variants side by side, and a rail there, being block content, stacks the pair into rows.) `tone` tints one chip — the layer's own vocabulary, `$tones.names`, which is `ok` green, `warn` amber, `danger` red — and is the author's judgement about this answer, not a property of the facet: `risk: high` is red because you say so, and the same word on another page may not be. Leave it off and the chip is neutral, which is what most of them should be; colour that lands on every chip has stopped pointing at anything. It is the same bargain `cq-metric` makes with `direction` — the theme knows the tints, the author knows which way is good.",
    "type": "object",
    "properties": {
      "id":   {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "tone": {"type": "string"}
    },
    "additionalProperties": false,
    "x-parent": ["cq-option", "cq-variant"],
    "x-content": "prose",
    "x-inline": true,
    "x-tone": "tone",
    "x-upgrade": false
  },
  "cq-milestones": {
    "description": "A timeline of milestones with status dots on a rail. Children are cq-milestone elements only.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": false,
    "x-example": "<cq-milestones>\n  <cq-milestone id=\"m-survey\" status=\"done\" when=\"week 1\">\n    <strong>Survey the sites</strong> Map sun and squirrel routes.\n  </cq-milestone>\n  <cq-milestone id=\"m-build\" status=\"active\" when=\"weeks 2-3\" tags=\"wood,solar\">\n    <strong>Build the feeders</strong> Two classic, two heated.\n  </cq-milestone>\n  <cq-milestone id=\"m-install\" status=\"planned\" when=\"week 4\">\n    <strong>Install and watch</strong>\n  </cq-milestone>\n</cq-milestones>"
  },
  "cq-milestone": {
    "description": "One milestone. Lead with a <strong> title; prose follows. `status` colors the timeline dot; `when` is a free-text schedule label; `tags` is a comma-separated list rendered as chips.",
    "type": "object",
    "properties": {
      "id":     {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "status": {"enum": ["planned", "active", "done", "blocked"]},
      "when":   {"type": "string"},
      "tags":   {"type": "string", "pattern": "^[a-z0-9-]+(,[a-z0-9-]+)*$"}
    },
    "required": ["id", "status"],
    "additionalProperties": false,
    "x-parent": ["cq-milestones"],
    "x-content": "prose",
    "x-upgrade": true
  },
  "cq-tasks": {
    "description": "A hierarchy of work: what a project is made of, who holds each piece, and where each piece stands. Children are cq-task elements, nesting freely — the tree is the plan's own shape, so write the decomposition as it is rather than flattening it. Where cq-milestones is a flat rail of stages on a schedule and cq-board is a set the reviewer re-orders, cq-tasks is structure: a parent's children are what doing it means, and the upgrade counts each parent's subtree into a generated done-fraction chip so the tree reads as progress without a widget of its own. Statuses change by republishing, or between versions by a worker's `colloquy report` (x-report on cq-task) — the page keeps up with the work — and comments anchor per task, so keep every task's id stable across versions.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": false,
    "x-example": "<cq-tasks id=\"aviary-plan\">\n  <cq-task id=\"t-feeders\" status=\"active\" owner=\"wren\">\n    <strong>Rebuild the feeders</strong> Two of four mounted.\n    <cq-task id=\"t-mounts\" status=\"done\" owner=\"wren\"><strong>Replace the mounts</strong></cq-task>\n    <cq-task id=\"t-baffles\" status=\"review\" owner=\"finch\" when=\"aug 4\"><strong>Fit squirrel baffles</strong></cq-task>\n  </cq-task>\n  <cq-task id=\"t-bath\" status=\"blocked\">\n    <strong>Heat the bird bath</strong> Waiting on the transformer pick below.\n  </cq-task>\n</cq-tasks>"
  },
  "cq-task": {
    "description": "One piece of the work. Lead with a <strong> title; prose follows, and child cq-task elements after the prose are its decomposition. `status` colors the marker: planned, active, blocked (stuck — the prose says on what), review (finished by whoever held it, awaiting the reader), done. `owner` names who holds it now — an agent, a session, a person — and `when` is a free-text schedule label; both render as chips after the title, with `tags` adding one chip per entry. A parent with children also gets a generated done-fraction chip counting its subtree. A task that needs the reader's word is `review` or `blocked`: those two are the standing asks a tree makes (see $awaits), counted in the banner and stepped to by `a` alongside every other thing the page is waiting on, so keep them honest — a task marked active that is really waiting on a decision is invisible exactly where it needs to be seen. They ask until a later version says otherwise: a tree has no reader action channel, so the reader answers in words and you answer by republishing the status. The agent side does have one — `status` is agent-settable (x-report): a worker holding a task reports where it stands (`colloquy report <page> <task-id> status status=review`), the page paints it as provisional news, and it stands until a version absorbs or overrules it (see $report; `overruled` is the overruling version's word).",
    "type": "object",
    "properties": {
      "id":     {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "status": {"enum": ["planned", "active", "blocked", "review", "done"]},
      "owner":  {"type": "string"},
      "when":   {"type": "string"},
      "tags":   {"type": "string", "pattern": "^[a-z0-9-]+(,[a-z0-9-]+)*$"},
      "overruled": {"type": "boolean"}
    },
    "required": ["id", "status"],
    "additionalProperties": false,
    "x-parent": ["cq-tasks", "cq-task"],
    "x-content": "prose",
    "x-upgrade": true,
    "x-awaits": {"when": {"status": ["review", "blocked"]}},
    "x-report": {
      "status": {
        "detail": {"type": "object", "properties": {"status": {"enum": ["planned", "active", "blocked", "review", "done"]}}, "required": ["status"], "additionalProperties": false},
        "unit": "widget",
        "record": {"kind": "value", "attr": "status", "value": "status"}
      }
    }
  },
  "cq-timeline": {
    "description": "A chronological event log — the incident-report shape. Children are cq-event elements only. Where cq-milestones tracks planned work by status, cq-timeline records what happened, when.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": false,
    "x-example": "<cq-timeline>\n  <cq-event id=\"e-alert\" at=\"09:14\" kind=\"warning\">\n    <strong>Feeder camera offline</strong> Battery alert from the north feeder.\n  </cq-event>\n  <cq-event id=\"e-swap\" at=\"09:40\" kind=\"action\">\n    <strong>Battery swapped</strong>\n  </cq-event>\n  <cq-event id=\"e-ok\" at=\"09:41\" kind=\"info\">\n    <strong>Camera back</strong> Feed resumed.\n  </cq-event>\n</cq-timeline>"
  },
  "cq-event": {
    "description": "One event on a timeline. Lead with a <strong> title; prose follows. `at` is a free-text time label; `kind` colors the dot.",
    "type": "object",
    "properties": {
      "id":   {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "at":   {"type": "string"},
      "kind": {"enum": ["info", "action", "warning", "failure"]}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-parent": ["cq-timeline"],
    "x-content": "prose",
    "x-says": {"at": "before"},
    "x-upgrade": false
  },
  "cq-compare": {
    "description": "Labeled variants — neutral exhibition, where cq-options is a decision. Children are cq-variant elements only. An exhibition is looked across where a decision is read down, so terse variants sit side by side; block content in any variant stacks the group into full-width rows like an options group’s cards, and a `<dl class=\"facts\">` after the label docks as an aligned rail.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": false,
    "x-example": "<cq-compare>\n  <cq-variant id=\"v-cedar\"><strong>Cedar</strong> Weathers silver; no sealant.</cq-variant>\n  <cq-variant id=\"v-pine\"><strong>Pine</strong> Cheaper; seal it yearly.</cq-variant>\n</cq-compare>"
  },
  "cq-variant": {
    "description": "One variant in a comparison. Lead with a <strong> label; any content follows, widgets included.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-parent": ["cq-compare"],
    "x-content": "prose",
    "x-upgrade": false
  },
  "cq-metrics": {
    "description": "A KPI row. Children are cq-metric elements only.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": false,
    "x-example": "<cq-metrics>\n  <cq-metric id=\"k-visits\" value=\"312\" delta=\"+41\" direction=\"up-good\">daily visits</cq-metric>\n  <cq-metric id=\"k-refills\" value=\"2.1/wk\" delta=\"+0.4\" direction=\"up-bad\">refills</cq-metric>\n  <cq-metric id=\"k-species\" value=\"14\">species seen</cq-metric>\n</cq-metrics>"
  },
  "cq-metric": {
    "description": "One metric: element text is the label, `value` the headline number, `delta` a signed change (leading + or -), `direction` says which sign is good (up-good, up-bad) so the delta colors honestly; without it the delta stays neutral.",
    "type": "object",
    "properties": {
      "id":        {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "value":     {"type": "string"},
      "delta":     {"type": "string", "pattern": "^[+-].+$"},
      "direction": {"enum": ["up-good", "up-bad"]}
    },
    "required": ["id", "value"],
    "additionalProperties": false,
    "x-parent": ["cq-metrics"],
    "x-content": "prose",
    "x-says": {"value": "before", "delta": "after"},
    "x-upgrade": false
  },
  "cq-diff": {
    "description": "A unified diff rendered with per-file collapse, add/del tints, and per-file +/− counts. The body is data: the diff verbatim inside its <pre>, at column 0 (context lines keep their leading space), with < and > escaped as &lt; and &gt;. Comments anchor to the required id. Takes no language: each file's own path is read for one ($languages), so keep the `diff --git` or ---/+++ headers rather than pasting bare hunks, and a path naming no language leaves that file plain.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-content": "data",
    "x-upgrade": true,
    "x-shadow": true,
    "x-example": "<cq-diff id=\"mount-diff\"><pre>\ndiff --git a/feeders/mount.py b/feeders/mount.py\n--- a/feeders/mount.py\n+++ b/feeders/mount.py\n@@ -1,3 +1,3 @@\n def bracket():\n-    return \"plastic\"\n+    return \"steel\"\n</pre></cq-diff>"
  },
  "cq-tree": {
    "description": "A file tree from an indent-notation body: one node per line, children indented deeper, a trailing / marking a directory, trailing +N/-N tokens rendering as change badges. Body is data — text inside its <pre>.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-content": "data",
    "x-upgrade": true,
    "x-example": "<cq-tree id=\"feeder-tree\"><pre>\nfeeders/\n  mount.py  +2 -2\n  schedule.py  +1\n  sites/\n    north.toml\n</pre></cq-tree>"
  },
  "cq-code": {
    "description": "An annotated code block — the walkthrough shape. The body is the code verbatim inside its <pre> (data, < and > escaped, column 0); `language` colors it, naming one of $languages; `hi` highlights line ranges (\"3-5,8\"); cq-note children sit beside that <pre> and anchor remarks at lines by number. Leave `language` off where the body isn't source (a transcript, command output) — nothing guesses, so an undeclared block stays plain.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "language": {"type": "string"},
      "hi": {"type": "string", "pattern": "^[0-9]+(-[0-9]+)?(,[0-9]+(-[0-9]+)?)*$"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-content": "data",
    "x-language": "language",
    "x-upgrade": true,
    "x-example": "<cq-code id=\"bracket-walk\" language=\"python\" hi=\"2\"><pre>\ndef bracket(temp):\n    if temp &lt; 0:\n        return \"steel\"\n    return \"cedar\"\n</pre>\n  <cq-note at=\"2\">Freezing is the only threshold that matters.</cq-note>\n</cq-code>"
  },
  "cq-note": {
    "description": "A remark anchored at a line of its cq-code parent. Prose content; `at` is the 1-based line number.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "at": {"type": "string", "pattern": "^[0-9]+$"}
    },
    "required": ["at"],
    "additionalProperties": false,
    "x-parent": ["cq-code"],
    "x-content": "prose",
    "x-upgrade": false
  },
  "cq-diagram": {
    "description": "A diagram rendered from a mermaid-source body (flowchart, sequence, state…). The body is data, not prose: text inside its <pre>, angle brackets escaped as &lt; and &gt;. Comments anchor to the required id, not to text inside the SVG.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-content": "data",
    "x-upgrade": true,
    "x-visual": true,
    "x-example": "<cq-diagram id=\"checkout-flow\"><pre>\ngraph LR\n  Cart --> Pay\n  Pay --> Receipt\n</pre></cq-diagram>"
  },
  "cq-shot": {
    "description": "A before/after pair of screenshots of one view, shown one at a time in a single frame: the user clicks between them and the change is the only thing on screen that moves. `before` and `after` are served image paths — `colloquy page media <page> <file>…` copies files into the page directory and prints the path to write, which is the only way an image gets onto a page (a screenshot is far too many bytes to inline). `alt` says what the pair shows, for a reader who can't see it. The element takes no content: what the page has to say about the change is ordinary prose written around it, where the user can quote it. Reach for it when the change reads at page scale — layout, spacing, a component swapped out. Below that scale the column's width is against you: crop the pair to the part that moved, or show the element itself before and after at real size.",
    "type": "object",
    "properties": {
      "id":     {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "before": {"type": "string", "pattern": "^/media/\\S+$"},
      "after":  {"type": "string", "pattern": "^/media/\\S+$"},
      "alt":    {"type": "string"}
    },
    "required": ["id", "before", "after", "alt"],
    "additionalProperties": false,
    "x-content": "none",
    "x-upgrade": true,
    "x-example": "<h3>The option cards</h3>\n<p>The chips now sit above the title, so a scanning eye reads cost before prose.</p>\n<cq-shot id=\"shot-cards\" alt=\"the decision page's option cards\" before=\"/media/0f2c91a4b7de3105.png\" after=\"/media/6b40ac7719ee5d82.png\"></cq-shot>"
  },
  "cq-board": {
    "description": "A kanban board the user edits directly: cards move between columns or reorder within one — dragged by the grip, or by keyboard (the grip is a button: Enter grabs, arrows move, Enter drops) — and each completed move reaches the agent as a `move` action naming the card, target column, and position. Children are cq-column elements only. Write the board as you mean it and leave it alone afterwards: a recorded move carries onto every later version by itself, so re-emitting a card where you first put it does not undo the user's move. Replay preserves the exact position, but the pending mark, version diff, and restatement gate compare a card's column only: a reorder within one column is deliberately not called out as a separate state change, because raw indexes do not compose when neighbouring cards move. A move rests on its card, so revising one they have moved is the card's `restated` (see $restated in `page catalog`), never the board's.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": true,
    "x-state": {
      "move": {
        "detail": {"type": "object", "properties": {"card": {"type": "string"}, "to": {"type": "string"}, "index": {"type": "integer", "minimum": 0}}, "required": ["card", "to", "index"], "additionalProperties": false},
        "unit": "card",
        "record": {"kind": "position", "within": "cq-column", "value": "to"}
      }
    },
    "x-example": "<cq-board id=\"feeder-board\">\n  <cq-column id=\"col-todo\" label=\"To do\">\n    <cq-card id=\"card-heater\"><strong>Heated perch</strong> Wire the south feeder.</cq-card>\n    <cq-card id=\"card-baffle\"><strong>Squirrel baffle</strong></cq-card>\n  </cq-column>\n  <cq-column id=\"col-doing\" label=\"Doing\">\n    <cq-card id=\"card-camera\"><strong>Camera mount</strong></cq-card>\n  </cq-column>\n  <cq-column id=\"col-done\" label=\"Done\"></cq-column>\n</cq-board>"
  },
  "cq-column": {
    "description": "One column on a board. `label` is the column heading, and the name a screen reader announces for the list of cards beneath it — so it has to read as the column's name on its own. Children are cq-card elements only; an empty column is a valid drop target.",
    "type": "object",
    "properties": {
      "id":    {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "label": {"type": "string"}
    },
    "required": ["id", "label"],
    "additionalProperties": false,
    "x-parent": ["cq-board"],
    "x-content": "items",
    "x-upgrade": false,
    "x-says": {"label": "before"}
  },
  "cq-card": {
    "description": "One card on a board. Lead with a <strong> title; prose follows. The user drags it by its grip; the id names it in `move` actions and keeps comment anchors stable across moves. Rewriting a card the user has moved needs `restated` on the card (see $restated in `page catalog`), which retracts the moves of that card alone — the rest of the board keeps its arrangement.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "restated": {"type": "boolean"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-parent": ["cq-column"],
    "x-content": "prose",
    "x-upgrade": false
  },
  "cq-tabs": {
    "description": "Parallel workstreams on one page: each cq-tab child is a self-contained sub-context (its own prose, boards, milestones) and a generated strip switches between them. The open tab is the reader's view state — remembered per browser tab like scroll position, never an action, never carried by a version — and the first panel is the default, so ordering carries emphasis. Find-in-page and comment-anchor jumps reach inactive tabs (the owning tab opens itself). Children are cq-tab elements only; unupgraded, panels stack as labeled sections.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": true,
    "x-example": "<cq-tabs id=\"aviary-projects\">\n  <cq-tab id=\"tab-feeders\" label=\"Feeders\">\n    <p>Rebuild underway; two of four feeders mounted.</p>\n  </cq-tab>\n  <cq-tab id=\"tab-bath\" label=\"Bird bath\">\n    <p>Heater options under review.</p>\n  </cq-tab>\n</cq-tabs>"
  },
  "cq-tab": {
    "description": "One workstream panel. `label` names its tab in the strip (and the stacked heading before upgrade); content is open — prose, sections, and other widgets nest freely. Ids inside share the page's one universe, so keep them distinct across tabs.",
    "type": "object",
    "properties": {
      "id":    {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "label": {"type": "string"}
    },
    "required": ["id", "label"],
    "additionalProperties": false,
    "x-parent": ["cq-tabs"],
    "x-content": "prose",
    "x-upgrade": false
  },
  "cq-suggestion": {
    "description": "One proposed change to content the user has already seen, decided in place. cq-old holds the current markup verbatim, cq-new the proposal: both slots replace, cq-new alone inserts, cq-old alone deletes. A widget-state change has no separate patch shape: put the complete old and new widget markup in those slots when one is genuinely needed. Pending, the old reads struck and the new tinted, with accept/reject controls in the page margin; deciding folds the retired half away and reaches the agent as an `accept` or `reject` action for the next version to carry. The control the reader pressed stays where it stood and says what it did (“✓ Accepted”), so the margin holds a record of what they took. Settlement is final on that version — that record is not an undo mark; a mistaken decision is corrected by a comment or the next version. `resolves` names the comment whose thread this change answers — accepting resolves that thread too. Suggestions don't nest, and a reply can't carry one (reply markup is frozen in the log, so no version could settle it). Fresh content — a new section, the first version — is written straight instead, and a block the user is to write in their own words is a cq-draft: theirs to rewrite, not the agent's to propose. The user can also just comment on a pending suggestion rather than decide it. An undecided one is a standing ask (see $awaits) — counted in the banner, stepped to by `a`, and the one kind a single press answers wholesale, which is the banner's ✓ Accept all.",
    "type": "object",
    "properties": {
      "id":       {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "resolves": {"type": "string", "pattern": "^[a-z0-9]+$"},
      "restated": {"type": "boolean"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-content": "items",
    "x-upgrade": true,
    "x-awaits": {"all": "accept"},
    "x-state": {
      "accept": {
        "detail": {"type": "object", "properties": {"resolves": {"type": "string"}}, "additionalProperties": false},
        "unit": "widget"
      },
      "reject": {
        "detail": {"type": "object", "additionalProperties": false},
        "unit": "widget"
      }
    },
    "x-example": "<p>Refill rules for the season:</p>\n<cq-suggestion id=\"sug-refill\">\n  <cq-old><p id=\"refill-rule\">Refill every feeder each morning.</p></cq-old>\n  <cq-new><p>Refill a feeder when its camera shows it half-empty.</p></cq-new>\n</cq-suggestion>"
  },
  "cq-old": {
    "description": "The markup a suggestion would replace or delete, verbatim — ids and all, which live here while the suggestion is pending. Omitted when the suggestion only inserts. Accepting the suggestion retires this slot: its words leave the page's reading, so nothing can anchor on them after.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "additionalProperties": false,
    "x-parent": ["cq-suggestion"],
    "x-content": "prose",
    "x-upgrade": false,
    "x-retired-when": "accept"
  },
  "cq-new": {
    "description": "The markup a suggestion proposes. Omitted when the suggestion only deletes. Rejecting the suggestion retires this slot: its words leave the page's reading, so nothing can anchor on them after.",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"}
    },
    "additionalProperties": false,
    "x-parent": ["cq-suggestion"],
    "x-content": "prose",
    "x-upgrade": false,
    "x-retired-when": "reject"
  },
  "cq-draft": {
    "description": "A block of text the user owns: they double-click it (or its ✎ button) to rewrite it in place, and the exact words reach the agent as an `edit` action naming the element. After the first edit, Changes shows a bounded, lossless word-aligned difference from this version, every recorded body in order, and a Restore control for each older body; very large divergent middles appear as one replacement rather than freezing the page. Restoring posts another ordinary `edit`, so it converges across tabs and later versions instead of creating private undo state. The body is the draft text verbatim inside its <pre> (data, < and > escaped; newlines preserved). It carries no decision chrome of its own — put a draft inside a cq-card where the column is the verdict, or let page sign-off approve the page as a whole. Usable anywhere prose flows. The agent rewriting prose of its own goes in a cq-suggestion instead; a draft is the user's own words, so there is nothing for them to accept. Once they have edited one those words are theirs: re-emit the body you wrote and their edit still stands over it — and `colloquy comment` reads it their way, quoting the words their edit left and refusing the ones it replaced. To take the pen back — a fact of yours to fix in a sentence they rewrote — write the new text and mark the element `restated` (see $restated in `page catalog`).",
    "type": "object",
    "properties": {
      "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "restated": {"type": "boolean"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-content": "data",
    "x-upgrade": true,
    "x-verbatim": true,
    "x-state": {
      "edit": {
        "detail": {"type": "object", "properties": {"text": {"type": "string"}}, "required": ["text"], "additionalProperties": false},
        "unit": "widget",
        "record": {"kind": "body", "value": "text"}
      }
    },
    "x-example": "<cq-draft id=\"note-cli\"><pre>\nAdds --dry-run to every mutating command.\n</pre></cq-draft>"
  },
  "cq-specimen": {
    "description": "Quoted material: a region the page exhibits rather than speaks — a sample decision, a demo board, markup shown as an example. Renders as quotation: a hairline left gutter under a small-caps \"specimen\" line (`label` extends it — \"specimen · a decision\"). Content is open and renders at full fidelity, but it is a mention, not a use: interactive widgets inside are inert — a choose group takes no clicks, a board no drags, a suggestion no decision. Keep the content visibly fictional, and let the gutter do the marking: a caption explaining that the region is an example is the page narrating a widget it could just use.",
    "type": "object",
    "properties": {
      "id":    {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$"},
      "label": {"type": "string"}
    },
    "required": ["id"],
    "additionalProperties": false,
    "x-content": "prose",
    "x-says": {"label": "before"},
    "x-exhibit": true,
    "x-upgrade": false,
    "x-example": "<cq-specimen id=\"spec-gear\" label=\"next season's kit\">\n  <cq-options>\n    <cq-option id=\"sp-paper\"><cq-chip>effort: low</cq-chip><strong>Paper maps</strong> Nothing to charge.</cq-option>\n    <cq-option id=\"sp-gps\" recommended><cq-chip>effort: med</cq-chip><strong>Dedicated GPS</strong> Offline maps, week-long battery.</cq-option>\n  </cq-options>\n</cq-specimen>"
  },
  "$languages": {
    "description": "The languages this page's layer can color, and what a filename means. `names` is the whole vocabulary: what an author may declare, what the lint refuses a word outside, and what scripts/vendor-highlight.sh builds the tokenizer bundle from. It is a property of the page rather than of any widget, which is why it lives here and not under the schema of whichever tag happens to take a `language` — a widget declares that one of its attributes names a language (x-language) and the lint reads the list from here, so a second tag taking one costs a declaration and no new reader. `paths` is the half nobody writes: the one place a language is derived rather than declared, because a unified diff spans files and so has no attribute to carry one. An extension is the diff's own statement about what a file holds, which is a declaration like any other; an extension absent here leaves that file plain.",
    "why-diff-is-not-a-path": "`diff` is in `names` and deliberately not in `paths`: a .patch inside a diff would be colored by the diff grammar, whose addition and deletion ink is the same statement the widget's own tints already make — said twice, in two channels, about the same line.",
    "names": ["bash", "css", "diff", "go", "html", "java", "javascript", "json", "markdown", "python", "rust", "sql", "toml", "typescript", "yaml"],
    "paths": {
      "bash": "bash", "cjs": "javascript", "css": "css", "go": "go", "htm": "html",
      "html": "html", "java": "java", "js": "javascript", "json": "json",
      "jsx": "javascript", "md": "markdown", "mjs": "javascript", "py": "python",
      "rs": "rust", "sh": "bash", "sql": "sql", "toml": "toml", "ts": "typescript",
      "tsx": "typescript", "yaml": "yaml", "yml": "yaml"
    }
  },
  "$events": {
    "description": "The vocabulary stamp: the event kinds this layer's runtime and CLI read, and the fields each carries beyond id/ts/author/kind/seq. Vendored with the page, it is the one statement of what that page's runtime speaks. `page init` refuses to re-vendor over a log recorded in kinds or verbs (x-state, per widget) the incoming layer no longer speaks, because those events would never replay again.",
    "kinds": {
      "comment": ["version", "text", "anchor", "suggestion", "agent", "session", "markup"],
      "reply": ["parent", "version", "text", "agent", "session", "markup"],
      "resolve": ["parent"],
      "done": ["version", "text"],
      "close": ["version"],
      "action": ["widget", "action", "detail", "version"],
      "report": ["widget", "action", "detail", "version", "agent", "session"],
      "note": ["version", "text", "restated", "reports", "agent", "session"]
    }
  },
  "$state": {
    "description": "What x-state declares, once per widget with an action channel: each verb's detail schema (the payload half of the log's forever-contract), its fold unit, and the record form its state takes in markup. Every applyAction is absolute, so the user's standing state is a fold — the last surviving action per unit — and one declaration drives the POST and re-vendor contract gates, `version check`'s state gate (a version whose markup actively contradicts the fold without `restated` is refused; writing the folded state is honoring; re-emitting the previous version's state is blessed silence), the record-lag report, the runtime's decided-but-unhonored mark, and the version diff's state half.",
    "unit": "\"widget\" for a verb absolute across the group (choose toggles every sibling), or the detail field naming the element the verb is absolute per (move's \"card\").",
    "record": "How the state reads in markup: {kind: \"attribute\", attr, value} — every element inside the unit carrying attr, sorted, vs the ids the detail field `value` lists, which is why that field is an array however many the widget allows at once; {kind: \"position\", within, value} — the enclosing `within` element, compared at that granularity (indexes compose across cards, so column is the whole comparison); {kind: \"body\", value} — the unit's own words; {kind: \"value\", attr, value} — the unit's own attr holding one scalar, whose detail field carries the attribute's own schema so the log's contract and the markup's cannot drift. A verb with no record (accept/reject) folds for the runtime's pending mark, and retirement stays retirable_ids' business."
  },
  "$report": {
    "description": "What x-report declares, once per widget with an agent channel: report verbs a worker folds onto the page through `colloquy report`, each with a detail schema (the payload half of the log's forever-contract, exactly as x-state's), a fold unit, and a required record form drawn from $state's vocabulary minus `body` — a report moves declared state only, never the page's words, which is what keeps the passage reading untouched by it. The runtime paints a standing report live (data-cq-reported) with precedence opposite to a reviewer's action: an action outranks every later version until `restated` retracts it; a report is provisional news, standing only until a version answers it.",
    "answering": "Explicit, by id. `version publish` names on the note (`reports`) every standing report the version answered: one whose recorded state the markup writes is absorbed, and one on an element marked `overruled` is overruled — the why belongs in the note's text. Replay stops a named report at that version. Silence — markup unchanged — is blessed and names nothing, so the report keeps painting. `version check` refuses the third thing, markup that contradicts a standing report it never names, and refuses an `overruled` with nothing to overrule, exactly as the reviewer gate polices `restated`.",
    "overruled": "A boolean attribute on the reported element, declared by every x-report widget: this version keeps its own state over the standing report. It belongs to the one version that does the overruling — publishing records the answer on the note, so later versions leave it off.",
    "wakes": "A report joins the watcher's batch: `colloquy wait` prints it beside user events, the same `ack` acknowledges it, and idling is refused over one nobody read. The reader's banner counts none of this — a report is news the agent owes the page, not something the reader owes an answer."
  },
  "$awaits": {
    "description": "What x-awaits declares: an instance of this tag is a standing request to the reader — a question with no pick, a change nobody has decided, a piece of work the page says is waiting on them. One declaration is the whole of it. The banner's count, the `a` key that steps them, and the `?` overlay's row are three readings of one list, so nothing counts asks by naming a tag and the twelfth widget joins by declaring rather than by being taught to three consumers. Quoted material asks nothing — an exhibited decision is a mention (x-exhibit). A widget inside a thread asks like one on the page — a question is a request to the reader wherever it stands, and `a` opens the panel to reach it — with one bound set by what a thread is: its markup is frozen, so no version can answer there, and only a widget with an action channel (x-state) asks in a thread at all.",
    "when": "The attribute condition that makes an instance an ask: each attribute mapped to the values of it that ask, a flag attribute's values being true and false. A group takes picks only with `choose` and stops asking once it is `settled`; a task waits only at `review` or `blocked`. An entry with no `when` asks on every instance, which is what a pending suggestion is.",
    "answered": "Never bookkeeping of its own — it is the state the widget already declares. Where a verb records its state as an attribute (x-state, record kind \"attribute\"), the page carries the answer itself, which is what lets a version that honors a pick read as answered with no log at all and a pick the reader cleared read as open again. Where a verb records nothing — accept and reject retire the wrapper, so no markup could hold them — the log is the only record there is, and the ask is answered by a surviving action naming it. A widget with no action channel can therefore only be answered by the next version, which is exactly what a task in `review` is waiting for.",
    "until": "The verb that closes a thread ask a record can't: an instance matching `until.when` stays asking, whatever its record state, until the reader has posted `until.verb` on it. It exists for the set-valued question — a `multiple` group's toggles each reach the agent live, so only the reader can say the set is whole, and the widget renders the press that says it (cq-options' Done). Consulted only in the comment layer: on the page the record answers as ever, versions can honor and retire, and no submit stands between a pick and the agent.",
    "all": "The verb a blanket yes takes, where one press can answer every ask of this kind at once: the banner carries one control per declared verb (`✓ Accept all (3)`), and the widget exposes a method of that name answering one instance, so the log records each decision separately rather than one blanket consent. Leave it off and the ask is answered where the reader is, one at a time."
  },
  "$restated": {
    "description": "A boolean attribute saying this version has rewritten words the user already decided on, so what they decided no longer applies and they are being asked again. It goes on the element the decision rests on — a cq-draft or cq-suggestion, or the cq-card or cq-option inside a board or group — and retracts every action that named it: a move names one card, so a rewritten card drops that card's moves, leaving the rest of the board arranged as the user left it; a pick names every option chosen at once, so a rewritten option takes back the whole answer it was part of.",
    "why": "A move, pick, edit or accept is recorded in the log against the version it was made on, and the page replays every recorded action onto every later version. A decision therefore survives a republish on its own — you never carry one into the next version's markup, and re-emitting a widget as you first wrote it does not undo it. The cost is that your markup cannot quietly overrule them either, which is what this attribute is for.",
    "when": "Only where a later version has to change what the user decided about: an error you found in a draft they rewrote, a picked option whose prose was wrong. Write the new words, mark that element, and say why in the note — to the user this reads as a decision undone, and the note is where a version explains itself. `version check` refuses a version that changes those words without it and a `restated` that retracts nothing, so it can be neither forgotten nor spent as a reflex; when it names the wrong element, the error says which one it wanted.",
    "once": "It belongs to the one version that does the rewriting. Publishing records the retraction in the log, so it holds for good — later versions leave the attribute off, and `version check` refuses one that repeats it.",
    "example": "<cq-draft id=\"note-cli\" restated><pre>\nAdds --dry-run to every mutating command. It defaults on for `purge`.\n</pre></cq-draft>"
  },
  "$tones": {
    "description": "The semantic tints this layer paints, and the whole vocabulary an author may declare: what the lint refuses a word outside, and what the theme has a rule for. It is a property of the page rather than of any widget, which is why it lives here and not under the schema of whichever tag happens to take a `tone` — a widget declares that one of its attributes names one (x-tone) and the lint reads the list from here, so a second tag taking a tone costs a declaration and no new reader. The list is short on purpose: a page that can paint anything paints nothing, and three is what the palette already answers for. Which facts earn a tint is never the theme's to decide — `ok` on one page's chip and `danger` on another's may be the same word, because the author is the only one who knows whether the answer is good news.",
    "names": ["ok", "warn", "danger"]
  },
  "$idioms": {
    "description": "Recurring shapes the theme styles directly — no registry entry, no JS. Plain semantic HTML gets the theme's voice for free; these classes cover what HTML lacks an element for.",
    ".eyebrow": {
      "description": "A small-caps label above a heading.",
      "example": "<p class=\"eyebrow\">design proposal</p>\n<h1>Widgets</h1>"
    },
    ".lede": {
      "description": "The opening paragraph, set slightly larger.",
      "example": "<p class=\"lede\">One paragraph saying what this page decides.</p>"
    },
    ".tag": {
      "description": "An inline pill label.",
      "example": "<span class=\"tag\">experimental</span>"
    },
    ".callout": {
      "description": "An aside set off from the prose — a tinted field with a hairline border.",
      "example": "<aside class=\"callout\">Rollback is one flag flip; the backfill is idempotent.</aside>"
    },
    ".facts": {
      "description": "A label/value grid for summary facts, as a definition list. Inside a cq-option or cq-variant it docks right as a fixed-width rail instead, aligning each item's scalars down the group.",
      "example": "<dl class=\"facts\">\n  <dt>Owner</dt><dd>payments</dd>\n  <dt>Rollout</dt><dd>behind a flag</dd>\n</dl>"
    },
    "ol.steps": {
      "description": "An ordered procedure with numbered markers.",
      "example": "<ol class=\"steps\">\n  <li>Backfill the new store.</li>\n  <li>Verify counts, then flip reads.</li>\n</ol>"
    },
    "table": {
      "description": "Rows of one shape, compared column by column: ceilings per plan, runs per configuration. Columns take the width of what they hold, and a table wider than the measure wraps its cells or, failing that, scrolls inside itself — so nothing in one needs a width. `<th scope=\"row\">` names a row in the page's own voice; a bare `<th>` is a column heading, set in small caps as one. A `<caption>` above the rows says what the columns mean where the section's heading doesn't. Reach for cq-metrics when the numbers are a handful of headline figures rather than a grid, and cq-options when the rows are alternatives the reader picks between.",
      "example": "<table id=\"tiers\">\n  <caption>Ceilings per plan, and what each is counted against.</caption>\n  <thead><tr><th>Plan</th><th class=\"num\">A minute</th><th>Counted against</th></tr></thead>\n  <tbody>\n    <tr><th scope=\"row\">Free</th><td class=\"num\">60</td><td>the token</td></tr>\n    <tr><th scope=\"row\">Team</th><td class=\"num\">600</td><td>the token</td></tr>\n  </tbody>\n</table>"
    },
    "td.num": {
      "description": "A cell holding a quantity, right-aligned so a column of them lines up on its last digit — the comparison a column of figures is there for. Say it on the column's heading cell too, or the heading hangs left of its own figures. Nothing is guessed from the text: \"60\" and \"60 sessions\" are the same shape and different things.",
      "example": "<tr><th scope=\"row\">Enterprise</th><td class=\"num\">6,000</td><td class=\"num\">12,000</td></tr>"
    },
    "details": {
      "description": "Evidence: a claim as the <summary>, its backing collapsed beneath. Native disclosure; find-in-page reaches the collapsed content.",
      "example": "<details>\n  <summary>The parser drops the slash on self-closed custom tags</summary>\n  <pre><code>document.querySelector(\"cq-metric\").textContent\n// → \"rest of the paragraph\"</code></pre>\n</details>"
    },
    "pre > code.language-*": {
      "description": "A literal the user can select and quote: a command to run, a config file, a snippet of output. Name the language to color it — one of $languages, the same list a widget's own language attribute is held to, and `version check` refuses one outside it. Leave the class off where the body isn't source; nothing is guessed from the text. Reach for cq-code instead when the block is a walkthrough: numbered lines, highlighted ranges, remarks anchored at a line.",
      "example": "<pre><code class=\"language-bash\">colloquy version check ~/.local/state/colloquy/pages/auth-diagnosis --render</code></pre>"
    }
  }
}
