{
  "lf-margin-element-gallery": {
    "description": "The developer gallery's complete static exhibit of page-edge action controls, disclosure controls, status indicators, and agent-ownership stages. It renders the canonical anatomy through the runtime's `marginElement` helper, but its specimens are disabled and record no gestures. Use it only in the feature gallery; ordinary pages demonstrate these roles through the live widgets that own them.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "none",
    "x-exhibit": true,
    "x-upgrade": true,
    "x-example": "<lf-margin-element-gallery id=\"margin-element-gallery\"></lf-margin-element-gallery>"
  }
}
