{
  "lf-merge-film": {
    "description": "An animated, scrubbable film of worktrunk's wt merge, drawn as one SVG in the page's colours, with flags that re-plot the run. Its commits, refs, worktrees and hook chips are commentable visual parts while they are on screen. Each paint dispatches a bubbling film-frame event naming the current step, its caption and the run's status per step.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "empty",
    "x-upgrade": true,
    "x-visual": {
      "prefixes": [
        "commit:",
        "ref:",
        "tree:",
        "hook:"
      ]
    },
    "x-space": "available"
  },
  "lf-merge-steps": {
    "description": "The steps of a merge film, told beside it. Its content is one authored <ol>: an <li data-chapter> per step, each with an <h3> title, a <p> saying what the step does, and an empty <p class=\"this-run\">. The module marks the step playing, how this run treats each step, and fills this-run with why a step was skipped or stopped; a click on a step plays the film named by `for` from it.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "for": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "required": [
      "id",
      "for"
    ],
    "additionalProperties": false,
    "x-content": "markup",
    "x-refers": {
      "for": {}
    },
    "x-upgrade": true
  },
  "lf-sort-film": {
    "description": "An animated, scrubbable trace of the merge sort Rust shipped in 1.15, drawn as one SVG in the page's palette: the slice, the scratch buffer, and the run stack with its collapse() checks. It indicates the source line each step executes in the lf-code excerpts its sort-code, head-code and merge-code attributes name, by the line's number in slice.rs. The current step, each slot of the slice, the scratch lane, the run stack and each collapse() clause are commentable visual parts.",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "sort-code": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "head-code": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "merge-code": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false,
    "x-content": "empty",
    "x-upgrade": true,
    "x-space": "wide",
    "x-visual": {
      "prefixes": [
        "v:",
        "lane:",
        "stack:",
        "check:",
        "moment:"
      ]
    },
    "x-refers": {
      "sort-code": {},
      "head-code": {},
      "merge-code": {}
    }
  }
}
